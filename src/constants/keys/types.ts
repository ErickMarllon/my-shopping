export type TKeys = {
  MODULE: string;
  AUTH_TOKENS: string;
  USER: string;
  RECOVERY_PASSWORD_DATA: string;
  RECOVERY_PASSWORD_UUID: string;
  MENUS: string;
  IMAGES: string;
  MODULES: string;
  PROVIDER: string;
  PERMISSIONS: string;
  THEME: string;
  LOGO: string;
};
